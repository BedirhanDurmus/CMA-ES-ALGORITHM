services:
  - type: web
    name: cma-es-algorithm
    env: python
    rootDir: final_sunum/web_app
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 120
    autoDeploy: true
